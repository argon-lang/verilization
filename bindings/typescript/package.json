{
  "name": "@verilization/compiler",
  "version": "0.1.0",
  "description": "Compiler for verilization.",
  "type": "module",
  "exports": {
    ".": {
      "node": "./bin/index_node.js",
      "default": "./bin/index_web.js"
    },
    "./compiler": "./bin/compiler.js"
  },
  "scripts": {
    "build-wasm": "cd ../c-api && cargo build --lib --release --target wasm32-unknown-unknown",
    "build": "npm run build-wasm && mkdirp bin && cpy --rename verilization_compiler.wasm ../../target/wasm32-unknown-unknown/release/verilization_bindings_c_api.wasm bin/ && cpy ../../compiler-cli-core/src/help.txt bin/ && tsc --project tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "cpy-cli": "^3.1.1",
    "mkdirp": "^1.0.4",
    "typescript": "^4.2.4"
  }
}
